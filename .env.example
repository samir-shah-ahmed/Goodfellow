# This .env is for pre-alpha testing only!

# Server
PORT=8000

# Postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=balanced_alpha
DB_USER=postgres
DB_PASSWORD=postgres

# Providers (optional to start; comment out if using only RSS)
NEWSAPI_KEY=your_newsapi_key